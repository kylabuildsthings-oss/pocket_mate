"use client";

import { PocketMateStubPage } from "@/components/pocketmate/PocketMateStubPage";

export default function TradePage() {
  return (
    <PocketMateStubPage
      title="Trade"
      description="Swap and yield surfaces with clear simulated vs read-only live data. Protocol integrations come after the mode toggle is global."
    />
  );
}
