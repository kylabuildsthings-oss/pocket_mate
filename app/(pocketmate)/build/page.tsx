"use client";

import { PocketMateStubPage } from "@/components/pocketmate/PocketMateStubPage";

export default function BuildPage() {
  return (
    <PocketMateStubPage
      title="Build"
      description="Strategy editor, mock backtests, and deployment preview. Freqtrade-style execution will plug in behind a stable UI shell."
    />
  );
}
